# Documentation Guidelines

- Store all project documentation inside the `/docs` directory.
- When a task requires updated docs, modify the existing Markdown file instead of creating duplicates.
- If a brand new doc is essential, create only one Markdown file for the task within `/docs`.
- Skip documentation changes entirely when they do not add value for the current task.

